<h2><%= @partner.title %></h2>

    <div class="cards row">
      <% @startups.each do |d| %>
      <div class="card col-xs-4">
        <%= image_tag d.image %>
        <h2><%= d.title %></h2>
        <p><%= d.description %></p>
      </div>
      <% end %>
    </div>
